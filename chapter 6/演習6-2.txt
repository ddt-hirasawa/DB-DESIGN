演習6-2 B-Treeインデックスは更新が発生することで時間経過により性能が劣化します。
これを防止するにはインデックスの再構成必要になりますが具体的にあげなさい。

インデックスの再構成を実行することで性能の劣化を防止します
具体的にデータベースの最適化ツールを使う

MYSQL : optimize インデクス;
ただし、複雑な問い合わせをする場合は使わない方がよい場合があるようです

PostGreSql : reindex インデクス;
ただし、サービスが稼働中にreindex を使うとselect 文がブロックされてしまい
応答がなくなってしまう？